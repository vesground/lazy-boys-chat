import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  foundUsers: {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    top: 2,
    left: -12,
  },
  column: {
    display: 'flex',
    flexDirection: 'column',
  }

});
